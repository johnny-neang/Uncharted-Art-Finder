<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Sacramento Artist Directory — Arden Fair UnchARTed · UpperCloud Studio</title>
<meta name="description" content="A curated directory of 21 Sacramento-area artists and art agencies, evaluated for the Arden Fair UnchARTed program by UpperCloud Studio.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=IBM+Plex+Sans:wght@300;400;500;600&family=Unbounded:wght@400;500;600&display=swap" rel="stylesheet">
<style>
/* ---------- UpperCloud Brand System (BG—2026.001) ---------- */
:root {
  /* Surfaces */
  --paper: #F5F3EE;
  --paper-2: #EBE7DF;
  --paper-3: #FBF8F2;
  --ink: #1A1A1A;
  --ink-2: #4A4A4A;
  --ink-3: #8A8A8A;
  --rule: rgba(26,26,26,0.15);
  --rule-soft: rgba(26,26,26,0.08);

  /* Inverse */
  --obsidian: #161310;
  --obsidian-2: #2A2522;

  /* Accents — landmarks, not decoration */
  --marigold: #E89A1C;
  --marigold-soft: rgba(232,154,28,0.12);
  --jade: #1F8A76;
  --jade-soft: rgba(31,138,118,0.10);
  --cyan: #23B4D8;

  /* Type families */
  --font-display: "DM Serif Display", "Iowan Old Style", Georgia, serif;
  --font-body: "IBM Plex Sans", system-ui, -apple-system, sans-serif;
  --font-label: "Unbounded", system-ui, sans-serif;

  /* Spacing scale (multiples of 4) */
  --s1: 4px;  --s2: 8px;  --s3: 12px; --s4: 18px;
  --s5: 24px; --s6: 28px; --s7: 36px; --s8: 48px;
  --s9: 64px; --s10: 72px;

  --shadow-card: 0 1px 0 rgba(26,26,26,0.04), 0 8px 24px -10px rgba(26,26,26,0.10);
  --shadow-thumb: inset 0 0 0 1px rgba(26,26,26,0.06);
}

* { box-sizing: border-box; }
html, body { margin: 0; padding: 0; }
body {
  font-family: var(--font-body);
  font-size: 14px;
  line-height: 1.55;
  color: var(--ink-2);
  background: var(--paper);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
}

/* ---------- Layout ---------- */
.shell { max-width: 1320px; margin: 0 auto; padding: 0 var(--s6); }

/* ---------- Top bar (header chrome — wordmark left, meta right) ---------- */
.topbar {
  display: flex; align-items: center; justify-content: space-between;
  padding: var(--s6) 0 var(--s5);
  border-bottom: 1px solid var(--rule-soft);
}
.wm img { height: 28px; display: block; }
.topbar-meta {
  display: flex; gap: var(--s7); align-items: baseline;
  font-family: var(--font-label); font-weight: 500;
  font-size: 10px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-3);
}
.topbar-meta b { color: var(--ink); font-weight: 500; }

/* ---------- Hero (cover-style) ---------- */
.hero {
  padding: var(--s10) 0 var(--s8);
  border-bottom: 1px solid var(--ink);
  position: relative;
}
.hero-figno {
  font-family: var(--font-label); font-weight: 500;
  font-size: 11px; letter-spacing: 0.22em; text-transform: uppercase;
  color: var(--marigold);
  margin-bottom: var(--s2);
}
.hero-doctype {
  font-family: var(--font-label); font-weight: 500;
  font-size: 11px; letter-spacing: 0.22em; text-transform: uppercase;
  color: var(--ink-2);
  margin-bottom: var(--s7);
}
.hero-grid {
  display: grid;
  grid-template-columns: 1fr 360px;
  gap: var(--s8);
  align-items: end;
}
.hero h1 {
  font-family: var(--font-display);
  font-weight: 400;
  font-size: clamp(48px, 7vw, 96px);
  line-height: 0.96;
  letter-spacing: -0.025em;
  margin: 0 0 var(--s4);
  color: var(--ink);
  max-width: 14ch;
}
.hero h1 em {
  font-style: italic; color: var(--jade);
}
.hero-lede {
  font-family: var(--font-body); font-weight: 300;
  font-size: 18px; line-height: 1.5; color: var(--ink);
  max-width: 56ch;
  margin: 0;
}
.hero-meta {
  border-top: 1px solid var(--ink);
  padding-top: var(--s5);
  display: grid; grid-template-columns: 1fr; gap: var(--s4);
}
.hero-meta dl { margin: 0; display: grid; grid-template-columns: 1fr; gap: var(--s4); }
.hero-meta dt {
  font-family: var(--font-label); font-weight: 500;
  font-size: 10px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-3); margin-bottom: var(--s1);
}
.hero-meta dd {
  margin: 0;
  font-family: var(--font-body); font-size: 16px; color: var(--ink);
}

/* ---------- Stats strip ---------- */
.stats {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  border-bottom: 1px solid var(--rule);
}
.stat {
  padding: var(--s5) var(--s5) var(--s4);
  border-right: 1px solid var(--rule-soft);
}
.stat:last-child { border-right: 0; }
.stat-num {
  font-family: var(--font-display);
  font-size: 48px;
  line-height: 1;
  color: var(--ink);
  margin-bottom: var(--s1);
  letter-spacing: -0.02em;
}
.stat-num em { font-style: italic; color: var(--marigold); }
.stat-lbl {
  font-family: var(--font-label); font-weight: 500;
  font-size: 10px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-3);
}

/* ---------- Filters / toolbar ---------- */
.toolbar {
  display: flex;
  align-items: center;
  gap: var(--s4);
  padding: var(--s5) 0 var(--s4);
  border-bottom: 1px solid var(--rule-soft);
  flex-wrap: wrap;
}
.toolbar-label {
  font-family: var(--font-label); font-weight: 500;
  font-size: 10px; letter-spacing: 0.22em; text-transform: uppercase;
  color: var(--ink-3);
  margin-right: var(--s1);
}
.chip {
  display: inline-flex; align-items: center; gap: var(--s2);
  padding: 6px var(--s3);
  border: 1px solid var(--rule);
  background: transparent;
  font-family: var(--font-label); font-weight: 500;
  font-size: 10px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-2);
  cursor: pointer;
  transition: all 0.15s;
  user-select: none;
  line-height: 1.6;
}
.chip:hover { color: var(--ink); border-color: var(--ink); }
.chip[aria-pressed="true"] {
  background: var(--ink); color: var(--paper); border-color: var(--ink);
}
.chip .chip-count {
  font-size: 9px; opacity: 0.6; font-weight: 400; letter-spacing: 0.1em;
}
.search-wrap { margin-left: auto; }
.search {
  border: 1px solid var(--rule);
  background: transparent;
  padding: 7px var(--s3);
  font-size: 13px;
  font-family: var(--font-body);
  color: var(--ink);
  width: 240px;
}
.search:focus { outline: none; border-color: var(--ink); }
.search::placeholder {
  font-family: var(--font-label); font-weight: 500;
  font-size: 10px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-3);
}

/* ---------- Section eyebrow (NN — LABEL — JADE RULE) ---------- */
.section-head {
  padding: var(--s8) 0 var(--s5);
  margin-bottom: var(--s6);
}
.eyebrow {
  display: flex; align-items: center; gap: var(--s4);
  margin-bottom: var(--s4);
}
.eyebrow .num {
  font-family: var(--font-label); font-weight: 500;
  font-size: 11px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--marigold);
}
.eyebrow .lbl {
  font-family: var(--font-label); font-weight: 500;
  font-size: 11px; letter-spacing: 0.22em; text-transform: uppercase;
  color: var(--ink);
}
.eyebrow .line { flex: 1; height: 1px; background: var(--jade); }
.eyebrow .meta {
  font-family: var(--font-label); font-weight: 500;
  font-size: 10px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-3);
}
.section-title {
  font-family: var(--font-display);
  font-size: 38px;
  font-weight: 400;
  line-height: 1.05;
  letter-spacing: -0.015em;
  margin: 0;
  color: var(--ink);
}
.section-title em { font-style: italic; color: var(--jade); }
.section-meta { font-family: var(--font-label); font-weight: 500; font-size: 10px; letter-spacing: 0.18em; text-transform: uppercase; color: var(--ink-3); }

/* ---------- Card grid ---------- */
.grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--s6);
  padding-bottom: var(--s10);
}
@media (max-width: 920px) {
  .grid { grid-template-columns: 1fr; }
  .hero-grid { grid-template-columns: 1fr; }
  .stats { grid-template-columns: repeat(2, 1fr); }
}

.card {
  background: var(--paper);
  border: 1px solid var(--rule);
  padding: var(--s5);
  display: flex; flex-direction: column;
  position: relative;
  transition: border-color 0.15s;
  box-shadow: var(--shadow-card);
}
.card:hover { border-color: var(--ink); }
.card.is-hidden { display: none; }

.card-head {
  display: flex; align-items: flex-start; justify-content: space-between;
  gap: var(--s4);
  margin-bottom: var(--s3);
}
.card-num {
  font-family: var(--font-label); font-weight: 500;
  font-size: 10px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--marigold);
  margin-bottom: var(--s1);
}
.card-name {
  font-family: var(--font-display);
  font-size: 28px;
  line-height: 1.05;
  font-weight: 400;
  margin: 0 0 var(--s1);
  letter-spacing: -0.015em;
  color: var(--ink);
}
.card-name em { font-style: italic; color: var(--jade); }
.card-sub {
  font-size: 12px; color: var(--ink-2);
  display: flex; gap: var(--s2); align-items: center; flex-wrap: wrap;
}

.tier-badge {
  display: inline-flex; align-items: center; gap: var(--s1);
  font-family: var(--font-label); font-weight: 500;
  font-size: 9px;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  padding: 4px var(--s2);
  white-space: nowrap;
  flex-shrink: 0;
  border: 1px solid;
}
.tier-1 { color: var(--marigold); border-color: var(--marigold); background: var(--marigold-soft); }
.tier-2 { color: var(--jade); border-color: var(--jade); background: var(--jade-soft); }
.tier-3 { color: var(--ink-2); border-color: var(--rule); background: transparent; }

.kind-pill {
  font-family: var(--font-label); font-weight: 500;
  font-size: 9px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-3); padding: 3px 0;
}

/* ---------- Thumb grid ---------- */
.thumb-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--s2);
  margin: var(--s1) 0 var(--s4);
}
.thumb {
  position: relative;
  aspect-ratio: 4 / 3;
  background: var(--paper-2);
  overflow: hidden;
  box-shadow: var(--shadow-thumb);
}
.thumb svg, .thumb img { width: 100%; height: 100%; display: block; }
.thumb img { object-fit: cover; object-position: center; }
.thumb-researching {
  width: 100%; height: 100%;
  display: flex; align-items: center; justify-content: center;
  background: var(--paper-2);
  color: var(--ink-3);
  font-family: var(--font-label); font-weight: 500;
  font-size: 9px; letter-spacing: 0.18em; text-transform: uppercase;
  text-align: center;
  line-height: 1.5;
  padding: var(--s3);
  border: 1px dashed var(--rule);
}
.thumb-researching span { display: block; }
/* "Second image not available" — a settled state, not still-researching */
.thumb-unavailable {
  background: var(--paper-3);
  border-style: solid;
  border-color: var(--rule-soft);
  color: var(--ink-3);
}
.thumb-label {
  position: absolute;
  left: var(--s2); bottom: var(--s2);
  font-family: var(--font-label); font-weight: 500;
  font-size: 8px;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  background: rgba(26, 26, 26, 0.82);
  color: var(--paper);
  padding: 3px 7px;
  backdrop-filter: blur(2px);
  -webkit-backdrop-filter: blur(2px);
}

/* ---------- Card body ---------- */
.card-summary {
  font-size: 13px;
  line-height: 1.55;
  color: var(--ink-2);
  margin: 0 0 var(--s4);
}
.card-meta-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--s3);
  font-size: 12px;
  margin-top: auto;
  padding-top: var(--s3);
  border-top: 1px solid var(--rule-soft);
}
.card-meta-cell { display: flex; flex-direction: column; gap: var(--s1); }
.card-meta-cell dt {
  font-family: var(--font-label); font-weight: 500;
  font-size: 9px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-3);
}
.card-meta-cell dd {
  font-family: var(--font-body); font-size: 12px; color: var(--ink); margin: 0;
}

.fit-bar {
  display: flex; align-items: center; gap: var(--s2);
}
.fit-dots { display: flex; gap: 2px; }
.fit-dot {
  width: 6px; height: 6px; background: var(--rule);
}
.fit-dot.on { background: var(--marigold); }
.fit-num { font-family: var(--font-label); font-weight: 500; font-size: 9px; letter-spacing: 0.18em; color: var(--ink-3); text-transform: uppercase; }

.card-foot {
  margin-top: auto;
  padding-top: var(--s4);
  border-top: 1px solid var(--rule-soft);
  display: flex; flex-direction: column;
  gap: var(--s3);
  font-size: 12px;
}
.card-foot-action {
  display: flex; align-items: center;
  gap: var(--s3);
}
.card-link {
  flex: 0 0 auto;
  max-width: 45%;
  color: var(--ink-2); text-decoration: none;
  border-bottom: 1px solid var(--rule);
  padding-bottom: 1px;
  font-family: var(--font-label); font-weight: 500;
  font-size: 10px; letter-spacing: 0.22em; text-transform: uppercase;
  white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
}
.card-link:hover { color: var(--jade); border-bottom-color: var(--jade); }
.card-link::after { content: " →"; }
.card-tags { display: flex; gap: var(--s1); flex-wrap: wrap; }
.tag {
  font-family: var(--font-label); font-weight: 500;
  font-size: 9px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-2); padding: 2px var(--s2);
  border: 1px solid var(--rule);
}

/* ---------- Footer ---------- */
.footer {
  border-top: 1px solid var(--ink);
  padding: var(--s8) 0 var(--s9);
  margin-top: var(--s5);
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: var(--s8);
  font-size: 13px;
  color: var(--ink-2);
}
.footer h4 {
  font-family: var(--font-label); font-weight: 500;
  font-size: 10px; letter-spacing: 0.22em; text-transform: uppercase;
  color: var(--ink); margin: 0 0 var(--s3);
}
.footer p { margin: 0 0 var(--s2); line-height: 1.55; }
.footer-meta {
  font-family: var(--font-label); font-weight: 500;
  font-size: 9px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-3);
}
.footer-meta b { color: var(--ink); font-weight: 500; }
.footer .pg {
  font-family: var(--font-display); font-size: 22px;
  letter-spacing: 0; text-transform: none; color: var(--ink);
}


/* ---------- Hidden-counter footer (entries excluded for lacking 2 real photos) ---------- */
.hidden-counter {
  margin: var(--s4) 0 var(--s5);
  padding: var(--s3) var(--s4);
  border: 1px dashed var(--rule);
  background: var(--paper-2);
  font-family: var(--font-label); font-weight: 500;
  font-size: 10px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-3);
  text-align: center;
}
.hidden-counter code {
  font-family: var(--font-body);
  font-size: 11px;
  letter-spacing: 0;
  text-transform: none;
  color: var(--ink-2);
  background: var(--paper);
  padding: 1px 6px;
  border: 1px solid var(--rule-soft);
}

/* ---------- Recent Candidates (rendered with unified .card style) ---------- */
.discoveries-section { padding: var(--s8) 0 0; }
.candidate-filter-row {
  display: flex; align-items: center; flex-wrap: wrap; gap: var(--s2);
  margin: var(--s4) 0 var(--s5);
}
.candidate-filter-chip {
  font-family: var(--font-label); font-weight: 500;
  font-size: 10px; letter-spacing: 0.18em; text-transform: uppercase;
  padding: 6px var(--s3);
  border: 1px solid var(--rule);
  background: transparent;
  color: var(--ink-2);
  cursor: pointer;
  transition: background-color 0.15s ease, color 0.15s ease, border-color 0.15s ease;
}
.candidate-filter-chip:hover { border-color: var(--ink); color: var(--ink); }
.candidate-filter-chip[aria-pressed="true"] { background: var(--ink); color: var(--paper); border-color: var(--ink); }
.candidate-filter-chip[data-state="add"][aria-pressed="true"]     { background: var(--marigold); border-color: var(--marigold); color: #fff; }
.candidate-filter-chip[data-state="watch"][aria-pressed="true"]   { background: var(--jade);     border-color: var(--jade);     color: #fff; }
.candidate-filter-chip[data-state="reject"][aria-pressed="true"]  { background: var(--ink-3);    border-color: var(--ink-3);    color: var(--paper); }
.candidate-filter-chip[data-state="pending"][aria-pressed="true"] { background: var(--ink);      border-color: var(--ink);      color: var(--paper); }
.candidate-filter-chip .chip-count { color: inherit; opacity: 0.55; margin-left: 6px; font-size: 9px; font-weight: 400; letter-spacing: 0.1em; }
.discovery-empty-state {
  padding: var(--s6); border: 1px dashed var(--rule);
  color: var(--ink-3); font-style: italic; text-align: center;
  display: none;
}
.discovery-empty-state.is-on { display: block; }
.submit-url-cta {
  display: inline-flex; align-items: center; gap: 8px;
  margin-top: var(--s4);
  padding: 10px 18px;
  border: 1px solid var(--jade);
  border-radius: 999px;
  font-family: var(--font-label); font-weight: 500;
  font-size: 11px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--jade);
  text-decoration: none;
  transition: background-color 0.15s ease, color 0.15s ease;
}
.submit-url-cta:hover { background: var(--jade); color: #fff; }
.submit-url-cta .plus { font-size: 16px; line-height: 1; margin-top: -2px; }
.submit-url-cta .hint { color: var(--ink-3); margin-left: 12px; font-size: 10px; letter-spacing: 0.14em; }

/* ---------- Card action button (Promote / Move back to Candidates) ---------- */
.card-action {
  flex: 1 1 auto;
  display: inline-flex; align-items: center; justify-content: space-between;
  gap: var(--s2);
  min-height: 40px;
  padding: 10px var(--s4);
  font-family: var(--font-label); font-weight: 500;
  font-size: 11px; letter-spacing: 0.22em; text-transform: uppercase;
  border: 1px solid var(--ink);
  background: var(--paper);
  color: var(--ink);
  text-decoration: none;
  transition: background-color 0.15s ease, color 0.15s ease, border-color 0.15s ease;
}
.card-action::after {
  content: "↗";
  font-size: 13px;
  letter-spacing: 0;
  margin-left: var(--s2);
  transition: transform 0.15s ease;
}
.card-action:hover { background: var(--ink); color: var(--paper); }
.card-action:hover::after { transform: translate(2px, -2px); }
.card-action:focus-visible { outline: 2px solid var(--marigold); outline-offset: 2px; }

.card-action-primary {
  background: var(--marigold);
  border-color: var(--marigold);
  color: #fff;
}
.card-action-primary:hover {
  background: var(--ink);
  border-color: var(--ink);
  color: var(--paper);
}

/* ---------- Card notes (per-card freeform note, saved to data/notes.json) ---------- */
.card-notes {
  display: flex; flex-direction: column; gap: var(--s2);
  margin-top: var(--s2);
  padding-top: var(--s3);
  border-top: 1px dotted var(--rule);
}
.card-notes-label {
  display: flex; align-items: center; justify-content: space-between;
  font-family: var(--font-label); font-weight: 500;
  font-size: 9px; letter-spacing: 0.22em; text-transform: uppercase;
  color: var(--ink-3);
}
.card-notes-label .note-dot {
  display: inline-block; width: 6px; height: 6px; border-radius: 50%;
  background: var(--marigold); margin-left: var(--s2); vertical-align: middle;
}
.card-note-input {
  width: 100%; box-sizing: border-box;
  min-height: 50px; resize: vertical;
  font-family: var(--font-body); font-size: 12px; line-height: 1.45;
  color: var(--ink); background: var(--paper-3);
  border: 1px solid var(--rule);
  padding: var(--s2) var(--s3);
  transition: border-color 0.15s ease, background-color 0.15s ease;
}
.card-note-input::placeholder { color: var(--ink-3); }
.card-note-input:focus { outline: none; border-color: var(--jade); background: #fff; }
.card-note-row { display: flex; align-items: center; gap: var(--s3); }
.card-note-save {
  flex: 0 0 auto;
  font-family: var(--font-label); font-weight: 500;
  font-size: 10px; letter-spacing: 0.18em; text-transform: uppercase;
  padding: 6px var(--s3); min-height: 30px;
  border: 1px solid var(--ink); background: var(--paper); color: var(--ink);
  cursor: pointer;
  transition: background-color 0.15s ease, color 0.15s ease, opacity 0.15s ease;
}
.card-note-save:hover { background: var(--ink); color: var(--paper); }
.card-note-save:focus-visible { outline: 2px solid var(--marigold); outline-offset: 2px; }
.card-note-save:disabled { opacity: 0.5; cursor: default; }
.card-note-status {
  font-family: var(--font-label); font-weight: 500;
  font-size: 9px; letter-spacing: 0.16em; text-transform: uppercase;
  color: var(--ink-3);
}
.card-note-status.ok { color: var(--jade); }
.card-note-status.err { color: #B3261E; }

/* ---------- Claude rec badge (candidate cards only) ---------- */
.rec-badge {
  display: inline-block;
  margin-left: var(--s2);
  font-family: var(--font-label); font-weight: 500;
  font-size: 9px; letter-spacing: 0.18em;
  text-transform: uppercase; padding: 2px var(--s2);
  border: 1px solid;
}
.rec-add      { color: var(--marigold); background: var(--marigold-soft); border-color: var(--marigold); }
.rec-watch    { color: var(--jade);     background: var(--jade-soft);     border-color: var(--jade); }
.rec-reject   { color: var(--ink-3);    background: transparent;          border-color: var(--rule); }
.rec-pending  { color: var(--ink-3);    background: transparent;          border-color: var(--rule); font-style: italic; }
.rec-archive  { color: var(--ink);      background: var(--paper-2);       border-color: var(--ink); }
.tier-unscored { background: var(--paper-2); color: var(--ink-3); }

/* ---------- Events ---------- */
.events-section { padding: var(--s8) 0 0; }
.event-list { list-style: none; padding: 0; margin: 0; }
.event-item {
  display: grid;
  grid-template-columns: 140px 1fr auto;
  gap: var(--s4); align-items: baseline;
  padding: var(--s4) 0;
  border-bottom: 1px solid var(--rule-soft);
}
.event-date {
  font-family: var(--font-label); font-weight: 500;
  font-size: 10px; letter-spacing: 0.22em; text-transform: uppercase;
  color: var(--marigold);
}
.event-title {
  font-family: var(--font-display); font-size: 22px; font-weight: 400;
  letter-spacing: -0.01em;
  color: var(--ink); margin: 0 0 var(--s1);
}
.event-snippet { font-size: 12px; color: var(--ink-2); margin: 0; }
.event-source {
  font-family: var(--font-label); font-weight: 500;
  font-size: 9px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-3);
  text-align: right;
}
.event-source a { color: inherit; text-decoration: none; border-bottom: 1px solid currentColor; }
.event-source a:hover { color: var(--ink); }

/* ---------- Print ---------- */
@media print {
  body { background: white; }
  .toolbar, .topbar { display: none; }
  .grid { gap: var(--s3); grid-template-columns: 1fr 1fr; }
  .card { box-shadow: none; break-inside: avoid; page-break-inside: avoid; }
  .hero { padding: var(--s5) 0; }
}

/* ---------- Empty state ---------- */
.empty {
  text-align: center; padding: var(--s10) var(--s5);
  font-family: var(--font-display); font-style: italic; font-size: 22px;
  color: var(--ink-3);
  display: none;
}
.empty.is-on { display: block; }
</style>
</head>
<body>

<div class="shell">

  <!-- Topbar — wordmark left, meta right -->
  <header class="topbar">
    <a class="wm" href="#" aria-label="UpperCloud Media">
      <img src="assets/uppercloud-logo.png" alt="UpperCloud" />
    </a>
    <div class="topbar-meta">
      <span><b>Field Brief</b> · No. 014</span>
      <span>Sacramento <b>·</b> CA</span>
      <span id="lastReviewed">Reviewed {{LAST_REVIEWED}}</span>
    </div>
  </header>

  <!-- Hero (cover) -->
  <section class="hero">
    <div class="hero-figno">FIG. 01 / DIRECTORY</div>
    <div class="hero-doctype">Field Brief — No. 014 · Public Art</div>
    <div class="hero-grid">
      <div>
        <h1>Sacramento <em>artist</em><br>directory.</h1>
        <p class="hero-lede">
          A working catalog of twenty-one Sacramento-area artists and art agencies,
          shortlisted and assessed for the Arden Fair UnchARTed program. Curated,
          sequenced, and graded by UpperCloud Studio for client use.
        </p>
      </div>
      <aside class="hero-meta">
        <dl>
          <div>
            <dt>Client</dt>
            <dd>Arden Fair · Sacramento</dd>
          </div>
          <div>
            <dt>Program</dt>
            <dd>UnchARTed · 2026 cycle</dd>
          </div>
          <div>
            <dt>Brief</dt>
            <dd>Indoor mural &amp; rotating installs</dd>
          </div>
          <div>
            <dt>Curator</dt>
            <dd>UpperCloud Studio</dd>
          </div>
        </dl>
      </aside>
    </div>
  </section>

  <!-- Stats -->
  <section class="stats">
    <div class="stat"><div class="stat-num">21</div><div class="stat-lbl">Total entries</div></div>
    <div class="stat"><div class="stat-num"><em>7</em></div><div class="stat-lbl">Tier-one priorities</div></div>
    <div class="stat"><div class="stat-num">11</div><div class="stat-lbl">Tier-two established</div></div>
    <div class="stat"><div class="stat-num">3</div><div class="stat-lbl">Agencies / curators</div></div>
    <div class="stat"><div class="stat-num">42</div><div class="stat-lbl">Visual references</div></div>
  </section>

  <!-- Filter toolbar -->
  <div class="toolbar" role="toolbar" aria-label="Filter directory">
    <span class="toolbar-label">Filter</span>
    <button class="chip" data-filter="all" aria-pressed="true">All <span class="chip-count">21</span></button>
    <button class="chip" data-filter="tier-1" aria-pressed="false">Tier 1 <span class="chip-count">7</span></button>
    <button class="chip" data-filter="tier-2" aria-pressed="false">Tier 2 <span class="chip-count">11</span></button>
    <button class="chip" data-filter="tier-3" aria-pressed="false">Agencies <span class="chip-count">3</span></button>
    <button class="chip" data-filter="mural" aria-pressed="false">Murals</button>
    <button class="chip" data-filter="studio" aria-pressed="false">Studio &amp; canvas</button>
    <button class="chip" data-filter="best-fit" aria-pressed="false">Best fit · 4+</button>
    <span class="search-wrap"><input class="search" type="search" id="searchInput" placeholder="Search · name, medium, work"></span>
  </div>

  <!-- Directory section header -->
  <div class="section-head">
    <div class="eyebrow">
      <span class="num">02</span>
      <span class="lbl">The Directory</span>
      <span class="line"></span>
      <span class="meta">Sequenced by tier &amp; fit · 21 entries</span>
    </div>
    <h2 class="section-title">The <em>directory</em>.</h2>
  </div>

  <!-- Cards grid -->
  <main class="grid" id="cardGrid"></main>

  <div class="empty" id="empty">Nothing matches that filter.</div>

  <p class="hidden-counter" id="directory-hidden-counter">{{HIDDEN_DIRECTORY}} directory entries have a second photo still in research — add URLs to <code>data/manual_artist_photos.json</code> to fill them in</p>

  <!-- Recent Candidates section (everything not yet promoted to the Directory) -->
  <section id="discoveries-section" class="discoveries-section" hidden>
    <div class="section-head">
      <div class="eyebrow">
        <span class="num">03</span>
        <span class="lbl">Recent Candidates</span>
        <span class="line"></span>
        <span class="meta" id="discoveries-meta">Surfaced by the daily scanner · scored for reference · you decide</span>
      </div>
      <h2 class="section-title">Recent <em>candidates</em>.</h2>
      <a class="submit-url-cta"
         href="https://github.com/johnny-neang/Uncharted-Art-Finder/issues/new?template=seed.yml"
         target="_blank" rel="noopener"
         title="Opens a GitHub Issue to submit a URL — picked up by the next /refresh">
        <span class="plus">+</span> Submit an artist URL
        <span class="hint">picked up on next refresh</span>
      </a>
    </div>
    <div class="candidate-filter-row" role="toolbar" aria-label="Filter candidates by Claude recommendation">
      <span class="toolbar-label">Filter by Claude rec</span>
      <button class="candidate-filter-chip" data-state="all" aria-pressed="true">All <span class="chip-count" data-cand-count="all">0</span></button>
      <button class="candidate-filter-chip" data-state="add" aria-pressed="false">Add <span class="chip-count" data-cand-count="add">0</span></button>
      <button class="candidate-filter-chip" data-state="watch" aria-pressed="false">Watch <span class="chip-count" data-cand-count="watch">0</span></button>
      <button class="candidate-filter-chip" data-state="reject" aria-pressed="false">Reject <span class="chip-count" data-cand-count="reject">0</span></button>
      <button class="candidate-filter-chip" data-state="pending" aria-pressed="false">Pending <span class="chip-count" data-cand-count="pending">0</span></button>
    </div>
    <div class="grid" id="discoveryGrid"></div>
    <div class="discovery-empty-state" id="discoveryEmpty">No candidates match this filter.</div>
    <p class="hidden-counter" id="candidates-hidden-counter">{{HIDDEN_CANDIDATES}} candidates have a second photo still in research</p>
  </section>

  <!-- Events section -->
  <section id="events-section" class="events-section" hidden>
    <div class="section-head">
      <div class="eyebrow">
        <span class="num">04</span>
        <span class="lbl">Upcoming Events</span>
        <span class="line"></span>
        <span class="meta">Sacramento art calendar · refreshed daily</span>
      </div>
      <h2 class="section-title">What's <em>on</em>.</h2>
    </div>
    <ul class="event-list" id="eventList"></ul>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div>
      <h4>About this brief</h4>
      <p>Compiled by UpperCloud Studio for the Arden Fair UnchARTed program. Each entry is graded on a four-axis fit score — style, scale, audience, availability — and accompanied by two visual references from the artist's recent portfolio.</p>
      <p class="footer-meta">Field Brief · No. 014 · Cycle 2026</p>
    </div>
    <div>
      <h4>How to use this</h4>
      <p>Filter by tier or medium to narrow shortlists. Each card links out to the artist's primary portfolio. The directory is self-contained — open offline, print, or share the file.</p>
      <p class="footer-meta">Visual references regenerated by the <code>/refresh</code> pipeline. Real photos populate as the daily scrape resolves them.</p>
    </div>
    <div>
      <h4>Contact</h4>
      <p>UpperCloud Studio<br>Sacramento, California<br>hello@uppercloud.studio</p>
      <p class="footer-meta"><b>Authored</b> · {{LAST_REVIEWED}} &nbsp;·&nbsp; <b>Version</b> · 1.0.0</p>
    </div>
  </footer>

</div>

<!-- ============================================================ -->
<!-- ARTIST DATA + SVG LIBRARY + RENDERER                         -->
<!-- ============================================================ -->
<script>
/* SVG art library — each artwork is a hand-tuned vector that
   reflects the artist's actual signature style based on portfolio research.
   Replace any of these with a real <img src="data:..."> via inject_images.py. */

/* {{SVG_LIBRARY}} */

/* ============================================================
   ARTIST DATA TABLE
   ============================================================ */

/* {{ARTISTS_DATA}} */


/* {{DISCOVERIES_DATA}} */
/* {{EVENTS_DATA}} */
/* {{NOTES_DATA}} */

/* ============================================================
   RENDER
   ============================================================ */

const grid = document.getElementById("cardGrid");

const REPO = "johnny-neang/Uncharted-Art-Finder";

// ----- Card notes state -----
// window.NOTES is baked in at build time from data/notes.json (the shared,
// durable source of truth). A localStorage cache holds this browser's recent
// edits so a saved note survives a reload before the next rebuild bakes it in.
const NOTES = (typeof window.NOTES !== "undefined" && window.NOTES) ? window.NOTES : {};
const NOTES_LS_KEY = "uaf_notes_cache_v1";

function loadNotesCache() {
  try { return JSON.parse(localStorage.getItem(NOTES_LS_KEY) || "{}") || {}; }
  catch (_) { return {}; }
}
function saveNotesCache() {
  try { localStorage.setItem(NOTES_LS_KEY, JSON.stringify(notesCache)); } catch (_) { /* quota/full */ }
}
// Effective note = whichever of the local cache and the baked note is newer.
function effectiveNote(slug) {
  const cached = notesCache[slug];
  const baked = NOTES[slug];
  if (cached && baked) return (cached.updated_at || "") >= (baked.updated_at || "") ? cached : baked;
  return cached || baked || null;
}
let notesCache = loadNotesCache();
// Drop any cached entry the committed build has already caught up to, so the
// cache can't indefinitely shadow a teammate's newer note.
(function reconcileNotesCache() {
  let changed = false;
  for (const slug of Object.keys(notesCache)) {
    const baked = NOTES[slug];
    if (baked && (baked.updated_at || "") >= (notesCache[slug].updated_at || "")) {
      delete notesCache[slug]; changed = true;
    }
  }
  if (changed) saveNotesCache();
})();

// ----- Unified card renderer (used for both Directory and Recent Candidates) -----

function renderThumb(t) {
  // Real photo? render <img>. Empty slot? render a typographic
  // "researching" placeholder — honest about the gap without inventing
  // any imagery. data_uri (candidate path), img (cached directory path).
  const src = (t && (t.data_uri || t.img)) || "";
  if (src) {
    return `<img src="${src}" alt="${(t.label || '').replace(/"/g, '&quot;')}" loading="lazy">`;
  }
  // Explicit placeholder (e.g. a removed duplicate) renders its note —
  // distinct from the optimistic "researching" treatment for a blank slot.
  if (t && t.placeholder && t.note) {
    const note = String(t.note).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
    return `<div class="thumb-researching thumb-unavailable"><span>${note}</span></div>`;
  }
  return `<div class="thumb-researching"><span>Researching<br>second photo</span></div>`;
}

function paddedThumbs(thumbs) {
  // Always render exactly 2 slots, padding with null for any missing.
  const arr = (thumbs || []).slice(0, 2);
  while (arr.length < 2) arr.push(null);
  return arr;
}

function actionButton(item) {
  // The anchor's href is the GitHub Issue fallback. Click handlers
  // intercept and POST to /api/promote or /api/demote first; the issue
  // URL is only followed if the API returns an error (e.g. PAT not set,
  // function not yet deployed, etc).
  const nameAttr = (item.name || "").replace(/"/g, '&quot;');
  if (item.mode === "directory") {
    const url = `https://github.com/${REPO}/issues/new?template=demote.yml&title=${encodeURIComponent("Demote: " + item.name)}&slug=${encodeURIComponent(item.slug)}`;
    return `<a class="card-action" href="${url}" target="_blank" rel="noopener"
      data-action="demote" data-slug="${item.slug}" data-name="${nameAttr}"
      title="Move ${nameAttr} back to candidates">Move back to Candidates</a>`;
  }
  const url = `https://github.com/${REPO}/issues/new?template=promote.yml&title=${encodeURIComponent("Promote: " + item.name)}&slug=${encodeURIComponent(item.slug)}`;
  return `<a class="card-action card-action-primary" href="${url}" target="_blank" rel="noopener"
    data-action="promote" data-slug="${item.slug}" data-name="${nameAttr}"
    title="Promote ${nameAttr} to directory">Promote to Directory</a>`;
}

function recBadge(item) {
  if (item.mode !== "candidate") return "";
  const rec = item.rec || "pending";
  return `<span class="rec-badge rec-${rec}">${rec}</span>`;
}

function notesBlock(item) {
  const note = effectiveNote(item.slug);
  const text = (note && note.text) ? note.text : "";
  const hasNote = !!text.trim();
  const esc = text.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
  const slugAttr = String(item.slug).replace(/"/g, "&quot;");
  return `
      <div class="card-notes" data-note-slug="${slugAttr}">
        <div class="card-notes-label">
          <span>Notes${hasNote ? '<span class="note-dot" title="Has a note"></span>' : ''}</span>
          <span class="card-note-status" data-note-status></span>
        </div>
        <textarea class="card-note-input" data-note-input rows="2" placeholder="Add a note…">${esc}</textarea>
        <div class="card-note-row">
          <button type="button" class="card-note-save" data-note-save>Save note</button>
        </div>
      </div>`;
}

function renderArtCard(item) {
  const tierClass = item.tier ? `tier-${item.tier}` : "tier-unscored";
  const tierLabel = item.tier === 1 ? "Tier 1 · Priority"
                   : item.tier === 2 ? "Tier 2 · Established"
                   : item.tier === 3 ? "Tier 3 · Agency"
                   : "Unscored";

  const thumbsHtml = paddedThumbs(item.thumbs).map(t => {
    const labelTxt = (t && t.label) ? t.label.replace(/</g, '&lt;') : '';
    return `<div class="thumb">${renderThumb(t)}${labelTxt ? `<div class="thumb-label">${labelTxt}</div>` : ''}</div>`;
  }).join("");

  const fitDots = Array.from({length: 5}, (_, i) =>
    `<span class="fit-dot${i < (item.fit || 0) ? ' on' : ''}"></span>`
  ).join("");

  const tagsHtml = (item.tags || []).map(t => `<span class="tag">${t}</span>`).join("");

  const searchBlob = [
    item.name || '', item.medium || '', item.summary || '', (item.tags || []).join(' ')
  ].join(' ').toLowerCase().replace(/"/g, '');

  return `
    <article class="card" data-mode="${item.mode}" data-slug="${item.slug}" data-tier="tier-${item.tier || 0}" data-tags="${(item.tags || []).join(' ')}" data-search="${searchBlob}" data-fit="${item.fit || 0}" data-rec="${item.rec || ''}">
      <header class="card-head">
        <div>
          <div class="card-num">No. ${String(item.n || 0).padStart(2,'0')}</div>
          <h3 class="card-name">${item.name}</h3>
          <div class="card-sub">
            <span class="kind-pill">${item.kind || '—'}</span>
            ${recBadge(item)}
          </div>
        </div>
        <span class="tier-badge ${tierClass}">${tierLabel}</span>
      </header>
      <div class="thumb-grid">${thumbsHtml}</div>
      <p class="card-summary">${item.summary || ''}</p>
      <dl class="card-meta-row">
        <div class="card-meta-cell">
          <dt>Medium</dt>
          <dd>${item.medium || '—'}</dd>
        </div>
        <div class="card-meta-cell">
          <dt>Arden Fair fit</dt>
          <dd class="fit-bar">
            <span class="fit-dots">${fitDots}</span>
            <span class="fit-num">${item.fit || 0}/5</span>
          </dd>
        </div>
      </dl>
      <p class="card-summary" style="font-size:12px;margin:10px 0 0;color:var(--ink-2);font-style:italic">${item.suitability || ''}</p>
      <div class="card-foot">
        <div class="card-foot-action">
          ${actionButton(item)}
          <a class="card-link" href="${item.primaryUrl}" target="_blank" rel="noopener">${item.primaryHost || item.primaryUrl}</a>
        </div>
        <div class="card-tags">${tagsHtml}</div>
        ${notesBlock(item)}
      </div>
    </article>
  `;
}

// ----- Adapters: shape ARTISTS / DISCOVERIES into the unified interface -----

function artistToUnified(a) {
  return {
    n: a.n,
    slug: a.slug,
    name: a.name,
    tier: a.tier,
    kind: a.kind,
    medium: a.medium,
    tags: a.tags || [],
    summary: a.summary,
    fit: a.fit,
    suitability: a.suitability,
    primaryUrl: a.primaryUrl,
    primaryHost: a.primaryHost,
    thumbs: a.thumbs || [],
    addedAt: a.addedAt,
    mode: "directory",
  };
}

function candidateToUnified(c, idx) {
  const s = c.score || {};
  let host = "";
  try { host = new URL(c.url).hostname; } catch (_) { /* malformed URL */ }
  return {
    n: idx + 1,
    slug: c.slug,
    name: c.name,
    tier: s.suggested_tier || 0,
    kind: s.kind || c.source_name || "Candidate",
    medium: s.medium || "—",
    tags: s.suggested_tags || [],
    summary: s.one_line_summary || c.seed_description || `Surfaced from ${c.source_name}. Awaiting score.`,
    fit: s.family_fit || 0,
    suitability: s.suitability || "",
    primaryUrl: c.url,
    primaryHost: host,
    thumbs: c.thumbs || (c.image ? [c.image] : []),
    addedAt: c.first_seen,
    mode: "candidate",
    rec: s.proceed_recommendation || "pending",
    confidence: s.confidence,
    sacramento: s.sacramento_connection,
  };
}

grid.innerHTML = ARTISTS.map(a => renderArtCard(artistToUnified(a))).join("");

/* ============================================================
   FILTERS + SEARCH
   ============================================================ */

const chips = document.querySelectorAll(".chip");
const empty = document.getElementById("empty");
const searchInput = document.getElementById("searchInput");

let activeFilter = "all";
let searchTerm = "";

function applyFilters() {
  // Scope to the directory grid — candidate cards use the same .card class
  // but have their own filter chip row.
  const cards = document.querySelectorAll("#cardGrid .card");
  let visible = 0;
  cards.forEach(c => {
    const tier = c.dataset.tier;
    const tags = c.dataset.tags;
    const fit = parseInt(c.dataset.fit, 10);
    const text = c.dataset.search;

    let matchFilter = true;
    if (activeFilter === "tier-1") matchFilter = tier === "tier-1";
    else if (activeFilter === "tier-2") matchFilter = tier === "tier-2";
    else if (activeFilter === "tier-3") matchFilter = tier === "tier-3";
    else if (activeFilter === "mural") matchFilter = tags.includes("mural");
    else if (activeFilter === "studio") matchFilter = tags.includes("studio");
    else if (activeFilter === "best-fit") matchFilter = fit >= 4;

    const matchSearch = !searchTerm || text.includes(searchTerm);
    const visibleNow = matchFilter && matchSearch;
    c.classList.toggle("is-hidden", !visibleNow);
    if (visibleNow) visible++;
  });
  empty.classList.toggle("is-on", visible === 0);
}

chips.forEach(c => c.addEventListener("click", () => {
  chips.forEach(x => x.setAttribute("aria-pressed", "false"));
  c.setAttribute("aria-pressed", "true");
  activeFilter = c.dataset.filter;
  applyFilters();
}));

searchInput.addEventListener("input", e => {
  searchTerm = e.target.value.trim().toLowerCase();
  applyFilters();
});

/* ============================================================
   CANDIDATES + EVENTS (data-driven sections)
   ============================================================ */

const DISCOVERIES = (typeof window.DISCOVERIES !== "undefined") ? window.DISCOVERIES : [];
const EVENTS = (typeof window.EVENTS !== "undefined") ? window.EVENTS : [];

// Effective Claude recommendation, with `existing` legacy normalized to `archive`.
function effectiveRec(d) {
  const raw = (d.manual_override || (d.score && d.score.proceed_recommendation) || "pending");
  return raw === "existing" ? "archive" : raw;
}

// ----- Candidate filter chips (Claude rec subsets) -----

let candidateFilter = "all";

function refreshCandidateCounts() {
  const counts = { all: 0, pending: 0, add: 0, watch: 0, reject: 0, archive: 0 };
  document.querySelectorAll("#discoveryGrid .card").forEach(card => {
    counts.all++;
    const rec = card.dataset.rec || "pending";
    if (counts[rec] !== undefined) counts[rec]++;
  });
  Object.keys(counts).forEach(k => {
    const el = document.querySelector(`.candidate-filter-chip [data-cand-count="${k}"]`);
    if (el) el.textContent = counts[k];
  });
}

function applyCandidateFilter() {
  let visible = 0;
  document.querySelectorAll("#discoveryGrid .card").forEach(card => {
    const rec = card.dataset.rec || "pending";
    const show = (candidateFilter === "all") ? true : (rec === candidateFilter);
    card.style.display = show ? "" : "none";
    if (show) visible++;
  });
  const empty = document.getElementById("discoveryEmpty");
  if (empty) empty.classList.toggle("is-on", visible === 0);
}

function wireCandidateFilterChips() {
  document.querySelectorAll(".candidate-filter-chip").forEach(chip => {
    chip.addEventListener("click", () => {
      document.querySelectorAll(".candidate-filter-chip").forEach(c => c.setAttribute("aria-pressed", "false"));
      chip.setAttribute("aria-pressed", "true");
      candidateFilter = chip.dataset.state;
      applyCandidateFilter();
    });
  });
}

function renderEventItem(e) {
  return `
    <li class="event-item">
      <div class="event-date">${e.date_raw || ""}</div>
      <div>
        <h4 class="event-title">${e.title}</h4>
        <p class="event-snippet">${(e.snippet || "").slice(0, 200)}</p>
      </div>
      <div class="event-source">
        <a href="${e.url}" target="_blank" rel="noopener">${e.source}</a>
      </div>
    </li>
  `;
}

(function mountCandidatesAndEvents() {
  const dGrid = document.getElementById("discoveryGrid");
  const dSection = document.getElementById("discoveries-section");
  if (DISCOVERIES.length > 0) {
    dGrid.innerHTML = DISCOVERIES.map((c, i) => renderArtCard(candidateToUnified(c, i))).join("");
    dSection.hidden = false;
    wireCandidateFilterChips();
    refreshCandidateCounts();
    applyCandidateFilter();
    document.getElementById("discoveries-meta").textContent =
      `${DISCOVERIES.length} candidate${DISCOVERIES.length === 1 ? "" : "s"} · ranked by Claude · you decide what joins the Directory`;
  }
  const eList = document.getElementById("eventList");
  const eSection = document.getElementById("events-section");
  if (EVENTS.length > 0) {
    eList.innerHTML = EVENTS.slice(0, 12).map(renderEventItem).join("");
    eSection.hidden = false;
  }
})();

/* ============================================================
   ONE-CLICK PROMOTE / DEMOTE
   Delegated handler that calls /api/promote or /api/demote.
   Falls back to opening the prefilled GitHub Issue on any error.
   ============================================================ */

(function wireActionButtons() {
  document.addEventListener("click", async (e) => {
    const btn = e.target.closest("[data-action='promote'], [data-action='demote']");
    if (!btn) return;
    // Let modifier-clicks (cmd/ctrl/shift, middle-click) behave normally
    if (e.metaKey || e.ctrlKey || e.shiftKey || e.altKey || e.button !== 0) return;
    e.preventDefault();

    const action = btn.dataset.action;
    const slug = btn.dataset.slug;
    const name = btn.dataset.name || slug;
    const card = btn.closest(".card");
    const originalText = btn.textContent;
    const fallbackUrl = btn.href;

    btn.textContent = "Working…";
    btn.style.pointerEvents = "none";
    btn.style.opacity = "0.65";

    try {
      const resp = await fetch(`/api/${action}`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ slug }),
      });
      let data = {};
      try { data = await resp.json(); } catch (_) { /* non-JSON body */ }

      if (resp.ok && data.ok) {
        if (card) {
          card.style.transition = "opacity 0.3s ease, transform 0.3s ease";
          card.style.opacity = "0";
          card.style.transform = "scale(0.96)";
          setTimeout(() => {
            card.remove();
            if (action === "promote") refreshCandidateCounts();
          }, 320);
        }
        return;
      }

      // API responded but with an error — fall back to GH Issue.
      const why = (data && data.error) ? data.error : `HTTP ${resp.status}`;
      console.warn(`[${action}] API failed: ${why} — opening GitHub Issue instead`);
      window.open(fallbackUrl, "_blank", "noopener");
    } catch (err) {
      console.warn(`[${action}] network error: ${err.message} — opening GitHub Issue instead`);
      window.open(fallbackUrl, "_blank", "noopener");
    } finally {
      btn.textContent = originalText;
      btn.style.pointerEvents = "";
      btn.style.opacity = "";
    }
  });
})();

/* ============================================================
   CARD NOTES
   Save a per-card note to /api/comment (persisted to data/notes.json).
   Optimistically cached in localStorage so it survives reload before the
   next rebuild bakes it into window.NOTES. Works on Directory + Candidate
   cards alike; same delegated pattern as Promote/Demote.
   ============================================================ */

(function wireNotes() {
  function setStatus(el, text, cls) {
    if (!el) return;
    el.textContent = text;
    el.className = "card-note-status" + (cls ? " " + cls : "");
    if (text.indexOf("Saved") === 0) {
      setTimeout(() => { if (el.textContent === text) el.textContent = ""; }, 4000);
    }
  }

  function updateNoteDot(notesEl, hasNote) {
    const labelSpan = notesEl.querySelector(".card-notes-label > span:first-child");
    if (!labelSpan) return;
    const existing = labelSpan.querySelector(".note-dot");
    if (hasNote && !existing) {
      const dot = document.createElement("span");
      dot.className = "note-dot";
      dot.title = "Has a note";
      labelSpan.appendChild(dot);
    } else if (!hasNote && existing) {
      existing.remove();
    }
  }

  async function saveNote(notesEl) {
    const slug = notesEl.dataset.noteSlug;
    const input = notesEl.querySelector("[data-note-input]");
    const statusEl = notesEl.querySelector("[data-note-status]");
    const btn = notesEl.querySelector("[data-note-save]");
    if (!slug || !input) return;

    const text = input.value.trim();
    const now = new Date().toISOString().replace(/\.\d{3}Z$/, "Z");

    // Optimistic local cache first, so the note survives an immediate reload.
    if (text) notesCache[slug] = { text, updated_at: now };
    else delete notesCache[slug];
    saveNotesCache();
    updateNoteDot(notesEl, !!text);

    setStatus(statusEl, "Saving…", "");
    if (btn) btn.disabled = true;
    try {
      const resp = await fetch("/api/comment", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ slug, text }),
      });
      let data = {};
      try { data = await resp.json(); } catch (_) { /* non-JSON body */ }
      if (resp.ok && data.ok) {
        if (data.updated_at && notesCache[slug]) {
          notesCache[slug].updated_at = data.updated_at;
          saveNotesCache();
        }
        setStatus(statusEl, "Saved ✓", "ok");
      } else {
        const why = (data && data.error) ? data.error : `HTTP ${resp.status}`;
        console.warn(`[note] save failed: ${why}`);
        setStatus(statusEl, "Saved locally · sync failed", "err");
      }
    } catch (err) {
      console.warn(`[note] network error: ${err.message}`);
      setStatus(statusEl, "Saved locally · offline", "err");
    } finally {
      if (btn) btn.disabled = false;
    }
  }

  // Click the Save button.
  document.addEventListener("click", (e) => {
    const btn = e.target.closest("[data-note-save]");
    if (!btn) return;
    const notesEl = btn.closest(".card-notes");
    if (notesEl) saveNote(notesEl);
  });

  // Cmd/Ctrl+Enter inside a note textarea also saves.
  document.addEventListener("keydown", (e) => {
    if (!((e.metaKey || e.ctrlKey) && e.key === "Enter")) return;
    const input = e.target.closest("[data-note-input]");
    if (!input) return;
    const notesEl = input.closest(".card-notes");
    if (notesEl) { e.preventDefault(); saveNote(notesEl); }
  });
})();

</script>
</body>
</html>
